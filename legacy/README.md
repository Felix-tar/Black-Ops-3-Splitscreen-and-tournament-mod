# Altbestand (Version 0.2.x)

Diese Dateien stammen aus der ersten Fassung der Mod und werden nicht mehr benutzt.
Sie liegen hier nur zur Nachvollziehbarkeit.

- `build.ps1`, `build-development.ps1` - alte Build-Skripte, ersetzt durch `build-dev.ps1` im Hauptordner.
- `ui/t6/lobby/lobbymenus.lua` - fremde Rekonstruktion einer Originaldatei, fehlerhaft, nicht verwenden.
- `scripts/`, `zone/` - alter Stand des Spielskripts und der Zonenrezepte, aktuell in `development/`.
